# ---
# tags: cyb, cell, config
# crystal-type: source
# crystal-domain: cyber
# ---
# cell — the unit cyb runs: a local cybergraph + the loop that turns signals
# into proven state. Standalone workspace for now (isolated build); cyb embeds
# it once the local loop is green.
[package]
name = "cell"
version = "0.1.0"
edition = "2024"
description = "a cyb cell — local cybergraph, applies and proves cyberlinks"
license = "Cyber"

[dependencies]
cybergraph = { path = "../../cybergraph" }

[workspace]
