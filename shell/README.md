# cyb

GUI shell for the immortal robot — Bevy worlds (terminal, browser, graph, sigma).

```bash
cargo run -p cyb          # from cyb workspace root
make dev                  # same
make dmg                  # macOS .app + DMG (release)
```

Binary name: `cyb`. Android cdylib: `libcyb.so`.
