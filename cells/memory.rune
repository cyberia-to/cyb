col(
  row(
    stat(query("particles"), "particles"),
    stat(query("bytes"), "bytes"),
    stat(query("links"), "links")
  ),
  table(
    tr("name", "focus", "size", "when"),
    query("table-body")
  )
)
