[package]
name = "cyb-stream"
version = "0.1.0"
edition = "2024"
description = "Typed framed byte-stream protocol — the cyb-stream wire format"
license = "Cyber"

[dependencies]
bytes = "1"

[dev-dependencies]
