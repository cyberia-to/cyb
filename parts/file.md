---
title: file
tags: cyb, particle
alias: cyb file
crystal-type: entity
crystal-domain: cyb
---
# file

`file = particle + data` ([[file]]). a particle is identity; a file is identity plus bytes. the bytes may be absent on this machine — the file still exists and still links; what is absent is the page. spacetime shows a file page only when a [[cyb/reference/spark|spark]] can draw the bytes.

address: `cyb://file/<hex>` when a spark can draw it. no spark → no file page; [[cyb/parts/now|now]] stands on `cyb://particle/<hex>` instead — the black hole: mass without visible body.

not an anatomy organ. a prysm molecule plus a spark surface. the cursor is now; brain and memory only move it.
