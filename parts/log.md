---
title: log
tags: cyb, core
alias: cyb log
crystal-type: entity
crystal-domain: cyb
---
# log

the history of every interaction — and it already exists in substance: the durable tade `~/cyb/graph.log` *is* the log. every cast, every answer, every act lands on the tade; nothing is forgotten. [[cyb/parts/time|time]] reads the log backward as the past half of its screen.

the log world is authored in rune (`cells/log.rune`): three census numbers (signals, links, weight) and a table of the chain — signal, step, n, wt — newest of this neuron first. the commander still runs underneath; nushell is the engine, the table is the record.

part of the [[cyb/anatomy|anatomy]] (time)
